# bugbonsai

## 0.2.0

### Minor Changes

- 874a6e1: Add API-versioned trusted plugins for namespaced reducers, framework adapters, package-manager providers, and failure oracles, with CLI/config loading, resume-safe fingerprints, a complete example, and author documentation.
- aff659e: Add adaptive ddmin mutation groups, persisted content-based rejection caching, and thorough-mode deep AST reduction for block statements, branches, class members, switch cases, objects, arrays, and JSX.
- 500d7d1: Prioritize reductions with a conservative JavaScript and TypeScript import graph, recursively prune JSONC configuration, harden workspace and lockfile handling, and validate real TypeScript, Vitest, Vite, and Jest workflows.
- df88760: Add content-addressed portability manifests, deterministic ZIP archives and checksums, recipient-side verification, portable Node command normalization, and optional Docker/GitHub sharing artifacts.
- b7167da: Add a guarded beta publication channel, external dogfood observations,
  evidence-backed compatibility scoreboard, structured feedback intake, and public
  beta launch material.
- a0c3cd2: Add `--fail-on-output` for reducing warnings and incorrect output from commands
  that exit successfully, including portable recipient verification.
- 05d2223: Preserve root and workspace-local dependency installations in ESM-safe snapshots, recreate workspace package links for every isolated candidate, recursively exclude dependency/cache trees from inventory, and always honor explicit install commands during clean validation.
- d5a112d: Resume the exact persisted reducer schedule under the original run budget, reuse isolated dependency preparation snapshots, expose progress and ETA metadata, and recover safely from interruptions across every engine phase.
- c38b1f6: Add a cross-platform compatibility corpus, enforceable performance budgets,
  fault-injection coverage, and guarded trusted-publication release gates.
- 3c35a97: Add project configuration, trusted custom failure oracles, evidence-based framework adapters, root-aware monorepo reduction, workspace manifest pruning, and nested Vitest/Jest test-structure reduction.

### Patch Changes

- e3088d5: Route prerelease onboarding through the current `beta` dist-tag, pin generated recipient verification commands to the producing BugBonsai version, materialize dependency snapshots for hermetic Next.js and Turbopack execution, and document the evidence and safety gates required before promoting BugBonsai to `latest`.
- c9adf8d: Recover failures that depend on safe gitignored build output, preserve existing workspace dependency trees with `--no-install`, prevent command-generated files from leaking into accepted reduction candidates, and keep explicit output oracles stable across platform-specific stack paths.
