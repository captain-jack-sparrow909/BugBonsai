packages:
  - "."

overrides:
  "js-yaml@3": 3.15.1
  "js-yaml@4": 4.3.1
  "nanoid@3": 3.3.17

allowBuilds:
  esbuild: true
  # Platform bindings are declared optional dependencies and locked below.
  # Do not let the fallback postinstall script fetch or prepare extra binaries.
  unrs-resolver: false
