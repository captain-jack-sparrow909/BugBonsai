---
"bugbonsai": patch
---

Recover failures that depend on safe gitignored build output, preserve existing workspace dependency trees with `--no-install`, prevent command-generated files from leaking into accepted reduction candidates, and keep explicit output oracles stable across platform-specific stack paths.
