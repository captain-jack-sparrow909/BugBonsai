---
"bugbonsai": patch
---

Recover failures that depend on safe gitignored build output, preserve existing workspace dependency trees with `--no-install`, and prevent command-generated files from leaking into accepted reduction candidates.
