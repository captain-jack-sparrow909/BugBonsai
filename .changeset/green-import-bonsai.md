---
"bugbonsai": minor
---

Prioritize reductions with a conservative JavaScript and TypeScript import graph, recursively prune JSONC configuration, harden workspace and lockfile handling, and validate real TypeScript, Vitest, Vite, and Jest workflows.
