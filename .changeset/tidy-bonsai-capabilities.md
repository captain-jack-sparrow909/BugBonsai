---
"bugbonsai": minor
---

Add project configuration, trusted custom failure oracles, evidence-based framework adapters, root-aware monorepo reduction, workspace manifest pruning, and nested Vitest/Jest test-structure reduction.
